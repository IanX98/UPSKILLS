import styled from 'styled-components';

const Center = (props) => {
    return <Container>
        Center
    </Container>
}

const Container = styled.div`
    grid-area: main;
`

export default Center;